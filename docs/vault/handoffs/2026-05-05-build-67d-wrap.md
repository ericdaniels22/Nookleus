---
date: 2026-05-05
build_id: 67d
session_type: focused
machine: Eric
related: ["[[build-67d]]", "[[build-66]]", "[[build-67c2]]"]
---

# Build 67d Handoff — 2026-05-05 (wrap)

## What shipped this session

- **Task 27 — orphan PDF cleanup.** Deleted `WTR-2026-0018-INV-3.pdf` from the `pdfs` Storage bucket via the Supabase JS Storage API (direct `DELETE FROM storage.objects` is blocked by `storage.protect_delete()` trigger). One-shot Node script `scripts/delete-orphan-inv3.ts` written, run via `npx tsx --env-file=.env.local`, deleted after the run. **`INV-2.pdf` intentionally preserved** — though listed as "orphan" in the 67d plan/handoff, a fresh draft invoice row `26124a3d-0b51-42f2-a74b-ab2b34074ef4` was created on 2026-05-02 and now references that exact PDF path. Deleting it would have broken the live row's PDF link. `EST-7.pdf` and `EST-9.pdf` also preserved (live rows). Final WTR-2026-0018 storage state: 3 PDFs, all backed by live rows.
- **Task 25 — §11 manual test pass via QA subagent.** Dispatched `general-purpose` subagent with self-contained brief (spec §11 verbatim + Test Co fixture inventory + fixture-prep gaps + Eric's user_id + identity discipline rules). Ran all 12 scenarios against prod Supabase Test Co. **12/12 PASS** at the DB+API+Storage layers; 27 `contract_events` audit rows captured with correct shapes. Reused 67c2 fixtures (`WTR-2026-T67C2` + estimates A–E + INV-A); approved + converted estimates as needed for Tests 3 + 9.1 + 11; created a 2nd job `WTR-2026-T67D-T10` (id `ee14016a-2be6-4fcf-bd3e-5e8b664bb828`) on Test Co contact `b1fdde59-2889-45c1-ba36-c91f128056c6` to satisfy Test 10's "different jobs" requirement. Eric demoted → admin restored cleanly for Test 8/9.8. AAA org untouched. Results doc landed at `docs/superpowers/specs/2026-05-05-build-67d-test-results.md`.
- **F1 fix landed inline (`056b633`)** — entity auto-save now terminal-stops on 404. Surfaced by §11 Test 7's "PUT returns 404 → terminal stop toast" sub-step. Existing per-line-item PUT path (`use-auto-save.ts:378-380`) and reorder paths already treated 404 as a stale signal; root entity path was missing the branch and would cycle exp-backoff retries indefinitely. Fix mirrors precedent in the same file (4 lines added).
- **Task 26 — `superpowers:code-reviewer` pass over the 67d diff** (range `e8fec69..056b633`, 30 commits, of which `8a8f594..056b633` are the substantive code commits). **APPROVED_WITH_FIXES** with 6 findings: 0 blocker, 3 important (I1 / I2 / I3), 1 nit (N1), 2 question (Q1 / Q2). Spec compliance walk-through confirmed every §4 deliverable, every §5 route, every §6 audit-payload field present.
- **I1 + I2 landed inline (`724eca3`).** I1: extended `.is("deleted_at", null)` filter to 3 accounting aggregations the Task 17 sweep missed (`accounting/summary` outstanding-AR card, `accounting/ar-aging` report, `accounting/export` AR aging CSV) — trashed-but-not-yet-purged invoices were inflating these for up to 30 days. I2: added `console.warn` on audit insert errors at all 4 hard-purge call sites (force-delete + lazy-purge for both estimate + invoice). Mirrors the existing soft-delete/restore pattern; closes a compliance hole where a failed audit insert silently stranded an irreversible destructive action.
- **I3 + N1 landed inline (`036edb8`).** I3: `/invoices` global dropdown now shows View/Edit/**Trash** (Void retired from UI per spec §2 Q5 + §12). Wired the existing `<TrashConfirmDialog>` from `src/components/trash/`. DELETE API endpoint preserved for any external callers per §12. Removed the now-unused `onVoid` callback. N1: `<TrashedBanner>` accepts optional `jobId` prop; estimate variant pushes to `/jobs/{jobId}` after force-delete (falls back to `/jobs` only if `jobId` is absent — defensive default). Invoice variant unchanged (still pushes to `/invoices`). Estimate read-only page passes `estimate.job_id` through.
- **Verification.** `npx tsc --noEmit` clean after every commit. Browser-verified I3 in Claude Preview: dropdown is `["View","Edit","Trash"]` (Void absent); clicking Trash opens TrashConfirmDialog with title `"Move invoice WTR-2026-T67C2-INV-3 to trash?"` + reason input + Cancel/Move-to-Trash buttons. Browser-verified N1: trashed estimate page renders `<TrashedBanner>` cleanly with no console errors after the prop change.
- **Two pushes this session** to `origin/main` triggering Vercel auto-deploys: first push covered F1 + I1 + I2 (`d3284d0..724eca3`), second push covered I3 + N1 (`724eca3..036edb8`).

## What's next

- **Vercel deploy verification.** Confirm the second push (`036edb8`) builds clean and reaches the "Current" badge before declaring 67d shipped to prod.
- **Q1 (deferred) — lazy 30-day auto-purge runs under `view_*` permission.** A view-only user visiting the trash page incidentally triggers a hard-purge of any expired row. Matches build-66 precedent (jobs/trash does the same), so it's an intentional pattern, but it diverges from spec §7's stated permission philosophy. Two paths if hardening: (a) move auto-purge to a daily cron decoupled from user requests, or (b) re-gate the lazy-purge loop on `manage_*` while keeping the list read on `view_*`. Worth a deliberate "tighten the permission model" build that revisits both jobs/trash and estimates/invoices/trash together — not a one-off.
- **F2 / Q2 (deferred) — audit `signer_id` left NULL on all 8 new audit-row inserts.** New rows store actor identity only as `metadata.actor_email`; `signer_id` uuid column has an FK to `contract_signers(id)` so `auth.users.id` cannot live there. Existing `estimate_sent` / `invoice_sent` audit rows from 67c2 also leave `signer_id` NULL. If hardening: add `metadata.actor_user_id: user?.id ?? null` alongside `actor_email` on every new audit insert (one-line change × 8 sites). Defense-in-depth against future `auth.users` row deletion / email changes.
- **C1 base-ui Dialog carry-over from 67c2 still applies.** TrashConfirmDialog + ForceDeleteConfirmDialog inherit the same "modal stays visually open after submit" quirk (`data-closed` flips but `display:grid` persists). Functionally correct (DB state changes, network 200, toast fires); UX papercut. Worth a fix when the underlying base-ui issue is addressed across all dialog usage.
- **AAA QB sandbox token expired** since 2026-04-21 (carry-over from prior sessions). Refreshing requires Eric's OAuth flow; once refreshed, run sync against `WTR-2026-0018-INV-3` to confirm `[CODE] description` shape end-to-end.
- **67c2 reviewer carry-overs F4–F8** still open: best-effort audit warn-only on send routes, renderer 4xx errors masked as 500, settings PATCH route org-blind (build40+ surface widened), `from_unconfigured` reply-shape inconsistency, migration trigger lacks `DROP TRIGGER IF EXISTS`, `BLOCKED_STATUSES` Set vs inline `=== "voided"` divergence, `MERGE_FIELDS` cross-domain coupling.
- **5xx error redactor sweep across the remaining ~80 routes** (mechanical apply pattern documented in [[build-67a]] follow-up).

## Decisions locked

- **Task 27 — delete only `INV-3.pdf`; preserve `INV-2.pdf`.** Re-verified state against the live DB before destructive action; INV-2 had a live draft row backing it. User explicitly confirmed "option 1" after the discovery was surfaced.
- **Task 25 — QA subagent authorized to mutate Test Co fixture state.** Convert estimates (EST-A, EST-C, EST-B), spin up a 2nd job for cross-job Test 10. User explicitly confirmed.
- **I3 — drop Void from `/invoices` UI entirely** (replace, don't supplement). Spec §2 Q5 ("Trash subsumes Void") + §12 ("UI no longer surfaces this directly") both unambiguous. DELETE API endpoint stays for external callers per §12. User explicitly confirmed.
- **F1 / I1 / I2 / I3 / N1 — land inline.** Each is a small mechanical change mirroring existing precedent in the same files. User explicitly authorized "lets do it. I3 and N1." after similar authorization for F1 and I1+I2 implicit in the §11 / Task 26 dispatch.
- **Q1 — defer to a future build.** Architectural pattern question, not a behavior bug; matches build-66 precedent. User explicitly confirmed "Q1 can stay as a future build entry."

## Open threads

- All 67d engineering threads now closed. The deferred Q1, F2, and C1 carry-over are the only structural items left from this build; each is non-blocking and queued above for a future hardening pass.
- Test Co fixture artifacts left in place (no cleanup required per QA subagent's brief): job `WTR-2026-T67D-T10` (`ee14016a-…`), invoice `T10-INV-1` (active draft, `e78a1c7c-…`), trashed `INV-2` from T3 (still in trash, ~29 days left), `INV-3 = 202d9bfd-…` (active draft from T11, FK SET NULL artifact). Available for follow-up testing or cleanup whenever convenient.

## Mechanical state

- **Branch:** main
- **Commit at session end:** `036edb8` (ui(67d): /invoices dropdown gets Trash, drops Void (I3) + estimate banner returns to parent job (N1))
- **Uncommitted changes:** 1 untracked file (`docs/superpowers/specs/2026-05-05-build-67d-preflight-capture.md` — intentionally transient, predates this session)
- **Migrations applied this session:** none (67d migration was applied in the execution session)
- **Deployed to Vercel:** yes — two pushes this session, both auto-deploys triggered. First push `d3284d0..724eca3` (covered F1 + I1 + I2). Second push `724eca3..036edb8` (covered I3 + N1).

## Notes for next session

- **Build 67d is now FULLY SHIPPED.** All 27 plan tasks landed (1-24 in execution session + 25/26/27 here) plus 5 follow-up code-review fixes (F1 from §11, I1+I2 from code-reviewer, I3+N1 from code-reviewer). `tsc --noEmit` clean throughout. §11 12/12 PASS. Code-reviewer APPROVED_WITH_FIXES with all fix-now landed.
- **The "INV-2.pdf is orphan" assumption from the 67d execution handoff went stale by the time this wrap session ran** — INV-2.pdf was re-claimed on 2026-05-02 by a fresh draft invoice row in AAA org. The wrap-session controller caught this via a SQL pre-flight before running the planned `DELETE FROM storage.objects` and stopped to confirm with Eric. Lesson: when a plan/handoff describes an "orphan" set (or any other "this row/file no longer matters"), re-verify against the live DB before destructive action. The 4-day gap between execution and wrap was enough for the assumption to drift.
- **`storage.protect_delete()` trigger blocks direct `DELETE FROM storage.objects`.** The error message is helpful (`"Direct deletion from storage tables is not allowed. Use the Storage API instead."`). The clean path is a small Node script using the `@supabase/supabase-js` service-role client, runnable via `npx tsx --env-file=.env.local scripts/<name>.ts`. Top-level `await` doesn't work because the runtime emits CJS — wrap in an `async function main(); main();`. Delete the script after the run.
- **The QA subagent's two findings (F1 + F2) and the code-reviewer's six findings (I1/I2/I3/N1/Q1/Q2) were complementary** — F2 and Q2 were the same finding, surfaced first by the QA subagent in the test-results doc and then re-evaluated by the code-reviewer (who agreed nit and noted the existing 67c2 audit rows have the same shape). When dispatching code-reviewer after a QA pass, brief it on the QA findings explicitly so it can re-evaluate rather than re-discover.
- **`scripts/delete-orphan-inv3.ts` is gone** but the pattern is reusable. Future Storage one-shots: copy the shape (load `NEXT_PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` from env, `createClient(url, key, { auth: { persistSession: false } })`, `service.storage.from(bucket).remove([path])`, async-wrap, log `{data, error}`). Existing scripts at `scripts/migrate-storage-paths.ts` and `scripts/seed-stripe-connection.ts` show similar patterns.
- **Code-reviewer subagent stat:** 1 dispatch, 72 tool uses, 227k tokens, ~6.4 min. Single fresh-context pass against the full 30-commit range produced a structured report with 6 well-cited findings + cross-cutting observations + spec-compliance walk-through. Worth replicating per build going forward.
- **Active workspace at session end:** Test Co (Eric is admin of both AAA + Test Co; QA subagent left workspace switched to Test Co for the §11 run; controller did not switch back).

## Links

- Build card: [[build-67d]]
- Current state: [[00-NOW]]
- This session's predecessors: [[2026-05-05-build-67d-execution]] (Tasks 1–24), [[2026-05-05-build-67d-planning]] (spec + 27-task plan)
- §11 results: `docs/superpowers/specs/2026-05-05-build-67d-test-results.md`
- Pattern reference: [[build-66]] (jobs soft-delete pattern that 67d mirrors)
